# ── Core dependencies ─────────────────────────────────────────────────────────
python-docx>=1.1.0       # Read .docx resume files
openai>=1.30.0           # OpenAI SDK  (needed if LLM_PROVIDER=openai)
anthropic>=0.28.0        # Anthropic SDK (needed if LLM_PROVIDER=anthropic)
python-dotenv>=1.0.0     # Auto-load .env file on startup

# ── Optional: real job scraping (uncomment whichever you use) ─────────────────
# google-search-results>=2.4.2   # SerpAPI (Option A)
# requests>=2.31.0               # Raw HTTP for Jsearch / RapidAPI (Option B)
# python-jobspy>=1.1.4           # Indeed + LinkedIn scraper (Option C)

# ── Standard library only — no extra install needed ──────────────────────────
# os, json, pathlib, argparse, datetime, re  →  all built-in
